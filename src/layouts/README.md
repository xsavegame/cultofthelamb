# Layouts

Layouts are reusable components that wrap around pages. They are used to provide a consistent look and feel across multiple pages.
This project currently applies `src/layouts/default.vue` as a global layout from `App.vue`, without `vite-plugin-vue-layouts-next`.
